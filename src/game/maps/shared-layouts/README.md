Save standalone `SharedMapLayout` modules in this folder to have them auto-discovered by `shared-map-layouts.ts`.

Map Lab's "Save Mission File" action is meant to write files here, for example:

- `src/game/maps/shared-layouts/my-test-mission.ts`

Each module should default-export a `SharedMapLayout`.
